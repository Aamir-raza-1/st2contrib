name: get_live_hosts
runner_type: run-python
description: List all hosts on a given Network
enabled: true
entry_point: get_live_hosts.py
parameters:
  subnet:
    type: string
    description: "Subnet of a network to scan for live Hosts(192.168.0.0/24)"
    required: true

